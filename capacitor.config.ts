import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lumen.game',
  appName: 'Lumen',
  webDir: 'dist',
  android: {
    backgroundColor: '#07070d',
  },
};

export default config;
